# deps
node_modules/
.pnpm-store/

# build
dist/
build/
.turbo/
*.tsbuildinfo

# typegen do React Router 8 (apps/web) — regenerado a cada dev/build/typecheck
.react-router/

# ambiente — NUNCA commitar (ver ADR-0011)
.env
.env.*
!.env.example

# artefatos nativos (vão para Releases/R2, não para o git)
apps/desktop/src-tauri/target/
apps/mobile/ios/
apps/mobile/android/
*.ipa
*.apk
*.aab
*.dmg
*.msi

# ferramentas
.DS_Store
coverage/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# turbo
.turbo/

# artefato intermediário do build de tokens (style-dictionary)
.tmp/

# storybook
storybook-static/

# Lighthouse CI — relatórios locais de cada run
.lighthouseci/
supabase/.temp/
