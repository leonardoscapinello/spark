import { useState } from "react"; import { useLiveQuery } from "@tanstack/react-db"; import { settingsControllerArchive, settingsControllerCreate } from "@spark/api-client"; import { customFieldDefinitionId, type CustomFieldDefinition, type CustomFieldEntity, type CustomFieldType } from "@spark/core"; import { ActionModal, Badge, Button, Checkbox, DataTable, Field, Input, Label, PageHeader, Select, notify, type TableColumn } from "@spark/ui-web"; import { getCustomFieldsCollection } from "../lib/custom-fields-collection.client"; import { requireCapability } from "../lib/route-access.client"; import styles from "./settings.module.css";
const ENTITY_OPTIONS = [{ value: "contact", label: "Contato" }, { value: "company", label: "Empresa" }, { value: "deal", label: "Negócio" }]; const TYPE_OPTIONS = [{ value: "text", label: "Texto" }, { value: "number", label: "Número" }, { value: "date", label: "Data" }, { value: "boolean", label: "Sim ou não" }, { value: "single_select", label: "Seleção única" }, { value: "multi_select", label: "Seleção múltipla" }];
export async function clientLoader() { await requireCapability("settings:manage"); void getCustomFieldsCollection().preload().catch(() => undefined); return null; }
export default function Settings() { const { data: fields, isLoading } = useLiveQuery({ query: (q) => q.from({ fields: getCustomFieldsCollection() }).orderBy(({ fields: item }) => item.createdAt, "desc") }); const [open, setOpen] = useState(false); const [busy, setBusy] = useState<string | null>(null); const [label, setLabel] = useState(""); const [key, setKey] = useState(""); const [entityType, setEntityType] = useState<CustomFieldEntity>("contact"); const [type, setType] = useState<CustomFieldType>("text"); const [required, setRequired] = useState(false); const [options, setOptions] = useState("");
  const columns: TableColumn<CustomFieldDefinition>[] = [{ id: "label", label: "Campo", cell: (item) => <div className={styles.primary}><strong>{item.label}</strong><span>{item.key}</span></div>, sortValue: (item) => item.label }, { id: "entity", label: "Registro", cell: (item) => labelFor(ENTITY_OPTIONS, item.entityType), sortValue: (item) => item.entityType }, { id: "type", label: "Tipo", cell: (item) => labelFor(TYPE_OPTIONS, item.type), sortValue: (item) => item.type }, { id: "required", label: "Regra", cell: (item) => <Badge tone={item.required ? "info" : "neutral"}>{item.required ? "Obrigatório" : "Opcional"}</Badge>, sortValue: (item) => item.required ? 1 : 0 }, { id: "status", label: "Status", cell: (item) => <Badge tone={item.archivedAt ? "neutral" : "success"}>{item.archivedAt ? "Arquivado" : "Ativo"}</Badge>, sortValue: (item) => item.archivedAt ?? "" }];
  async function create() { const normalizedOptions = options.split(",").map((item) => item.trim()).filter(Boolean); await settingsControllerCreate({ id: customFieldDefinitionId.create(), entityType, key: key.trim(), label: label.trim(), type, required, options: normalizedOptions }); notify({ title: "Campo criado", description: `${label} já está disponível nos registros.`, tone: "success" }); setLabel(""); setKey(""); setOptions(""); setRequired(false); }
  async function toggle(item: CustomFieldDefinition) { setBusy(item.id); try { await settingsControllerArchive(item.id, { archived: !item.archivedAt }); notify({ title: item.archivedAt ? "Campo restaurado" : "Campo arquivado", tone: "success" }); } finally { setBusy(null); } }
  return <div className={styles.page}><PageHeader eyebrow="Administração" title="Configurações" description="Defina a estrutura de dados usada em todo o CRM sem criar schema em runtime." actions={<Button onClick={() => setOpen(true)}>Novo campo</Button>} /><DataTable label="Campos personalizados" rows={fields} columns={columns} rowKey={(item) => item.id} rowLabel={(item) => item.label} state={isLoading && !fields.length ? "loading" : "ready"} emptyText="Nenhum campo personalizado." actions={(item) => <Button size="sm" variant="ghost" loading={busy === item.id} onClick={() => void toggle(item)}>{item.archivedAt ? "Restaurar" : "Arquivar"}</Button>} /><ActionModal open={open} onOpenChange={setOpen} title="Novo campo personalizado" confirmLabel="Criar campo" errorText="Revise o nome, a chave e as opções." onConfirm={create}><div className={styles.form}><Field><Label>Registro</Label><Select label="Tipo de registro" value={entityType} options={ENTITY_OPTIONS} onValueChange={(value) => setEntityType((value ?? "contact") as CustomFieldEntity)} /></Field><Field><Label>Nome visível</Label><Input value={label} placeholder="Número de funcionários" onChange={(event) => { const next = event.target.value; setLabel(next); if (!key) setKey(slugKey(next)); }} /></Field><Field><Label>Chave técnica</Label><Input value={key} placeholder="numero_funcionarios" onChange={(event) => setKey(slugKey(event.target.value))} /></Field><Field><Label>Tipo</Label><Select label="Tipo do campo" value={type} options={TYPE_OPTIONS} onValueChange={(value) => setType((value ?? "text") as CustomFieldType)} /></Field>{(type === "single_select" || type === "multi_select") && <Field><Label>Opções separadas por vírgula</Label><Input value={options} placeholder="Pequena, Média, Grande" onChange={(event) => setOptions(event.target.value)} /></Field>}<Checkbox checked={required} onCheckedChange={(checked) => setRequired(checked === true)}>Preenchimento obrigatório</Checkbox></div></ActionModal></div>; }
function labelFor(options: Array<{ value: string; label: string }>, value: string) { return options.find((item) => item.value === value)?.label ?? value; } function slugKey(value: string) { return value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9]+/g, "_").replace(/^_+|_+$/g, ""); }
