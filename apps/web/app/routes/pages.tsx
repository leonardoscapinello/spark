import { useState } from "react"; import { useLiveQuery } from "@tanstack/react-db"; import { useNavigate } from "react-router"; import { pagesControllerCreate } from "@spark/api-client"; import { pageId, type Page } from "@spark/core"; import { ActionModal, Badge, Button, DataTable, Field, Input, Label, PageHeader, notify, type TableColumn } from "@spark/ui-web"; import { getPagesCollection } from "../lib/pages-collections.client"; import { getSession } from "../lib/auth.client"; import { requireCapability } from "../lib/route-access.client"; import styles from "./pages.module.css";
export async function clientLoader() { await requireCapability("pages:read"); void getPagesCollection().preload().catch(() => undefined); return null; }
export default function Pages() { const navigate = useNavigate(); const canWrite = getSession()?.capabilities.includes("pages:write") ?? false; const { data: pages, isLoading } = useLiveQuery({ query: (q) => q.from({ pages: getPagesCollection() }).orderBy(({ pages: item }) => item.updatedAt, "desc") }); const [open, setOpen] = useState(false); const [name, setName] = useState(""); const [slug, setSlug] = useState(""); const columns: TableColumn<Page>[] = [{ id: "name", label: "Página", cell: (item) => <div className={styles.primary}><strong>{item.name}</strong><span>/{item.slug}</span></div>, sortValue: (item) => item.name }, { id: "status", label: "Status", cell: (item) => <Badge tone={item.status === "published" ? "success" : "neutral"}>{item.status === "published" ? "Publicada" : "Rascunho"}</Badge>, sortValue: (item) => item.status }, { id: "updated", label: "Atualizada", cell: (item) => new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeStyle: "short" }).format(new Date(item.updatedAt)), sortValue: (item) => item.updatedAt }]; async function create() { const response = await pagesControllerCreate({ id: pageId.create(), name: name.trim(), slug: slug.trim() }); notify({ title: "Página criada", tone: "success" }); navigate(`/pages/${response.page.id}`); } return <div className={styles.page}><PageHeader eyebrow="Aquisição" title="Páginas" description="Monte landing pages por blocos e publique versões imutáveis com a mesma renderização da prévia." actions={canWrite ? <Button onClick={() => setOpen(true)}>Nova página</Button> : undefined} /><DataTable label="Landing pages" rows={pages} columns={columns} rowKey={(item) => item.id} rowLabel={(item) => item.name} state={isLoading && !pages.length ? "loading" : "ready"} emptyText="Nenhuma página criada." actions={(item) => <Button size="sm" variant="ghost" onClick={() => navigate(`/pages/${item.id}`)}>Editar</Button>} /><ActionModal open={open} onOpenChange={setOpen} title="Nova página" confirmLabel="Criar e editar" errorText="Informe nome e endereço válidos." onConfirm={create}><div className={styles.form}><Field><Label>Nome interno</Label><Input value={name} placeholder="Landing de campanha" onChange={(event) => { setName(event.target.value); if (!slug) setSlug(toSlug(event.target.value)); }} /></Field><Field><Label>Endereço</Label><Input value={slug} placeholder="landing-de-campanha" onChange={(event) => setSlug(toSlug(event.target.value))} /></Field></div></ActionModal></div>; }
function toSlug(value: string) { return value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, ""); }
