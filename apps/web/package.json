{
  "name": "@spark/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "build": "tsc",
    "dev": "echo \"apps/web — dev server chega no bloco que implementa este app\"",
    "test": "echo \"sem testes ainda — apps/web\""
  },
  "devDependencies": {}
}
