import { INACTIVE_COLLECTION_GC_MS } from "./collection-lifecycle.js";
import { createCollection } from "@tanstack/react-db"; import { electricCollectionOptions } from "@tanstack/electric-db-collection"; import { PageSchema, PageVersionSchema } from "@spark/core"; import { sparkShapeOptions } from "./shape-options.js"; export function createPagesCollection() { return createCollection(electricCollectionOptions({ gcTime: INACTIVE_COLLECTION_GC_MS, id: "pages", schema: PageSchema, getKey: (item) => item.id, shapeOptions: sparkShapeOptions("pages") })); } export function createPageVersionsCollection() { return createCollection(electricCollectionOptions({ gcTime: INACTIVE_COLLECTION_GC_MS, id: "page_versions", schema: PageVersionSchema, getKey: (item) => item.id, shapeOptions: sparkShapeOptions("page_versions") })); } export type PagesCollection = ReturnType<typeof createPagesCollection>; export type PageVersionsCollection = ReturnType<typeof createPageVersionsCollection>;
