import { createCollection } from "@tanstack/react-db"; import { electricCollectionOptions } from "@tanstack/electric-db-collection"; import { snakeCamelMapper } from "@electric-sql/client"; import { CustomFieldDefinitionSchema } from "@spark/core"; import { getSparkApiBaseUrl, getSparkAuthToken } from "@spark/api-client";
export function createCustomFieldsCollection() { return createCollection(electricCollectionOptions({ id: "custom_field_definitions", schema: CustomFieldDefinitionSchema, getKey: (item) => item.id, shapeOptions: { url: `${getSparkApiBaseUrl()}/v1/shapes/custom_field_definitions`, columnMapper: snakeCamelMapper(), headers: { authorization: () => { const token = getSparkAuthToken(); return token ? `Bearer ${token}` : ""; } } } })); } export type CustomFieldsCollection = ReturnType<typeof createCustomFieldsCollection>;
