import { z } from "zod"; import { zOrgId, zPageId, zPageVersionId, zServerTimestamp, zUserId } from "./zodHelpers.js";
const BaseBlockSchema = z.object({ id: z.string().uuid() });
export const PageBlockSchema = z.discriminatedUnion("type", [BaseBlockSchema.extend({ type: z.literal("hero"), props: z.object({ eyebrow: z.string().max(100), title: z.string().min(1).max(200), text: z.string().max(1000), align: z.enum(["left", "center"]) }) }), BaseBlockSchema.extend({ type: z.literal("text"), props: z.object({ heading: z.string().max(200), text: z.string().max(5000), align: z.enum(["left", "center"]) }) }), BaseBlockSchema.extend({ type: z.literal("image"), props: z.object({ url: z.url(), alt: z.string().max(300) }) }), BaseBlockSchema.extend({ type: z.literal("button"), props: z.object({ label: z.string().min(1).max(80), url: z.string().min(1).max(2000), align: z.enum(["left", "center"]) }) }), BaseBlockSchema.extend({ type: z.literal("spacer"), props: z.object({ size: z.enum(["small", "medium", "large"]) }) })]);
export type PageBlock = z.infer<typeof PageBlockSchema>; export const PageTreeSchema = z.object({ blocks: z.array(PageBlockSchema).max(100) }); export type PageTree = z.infer<typeof PageTreeSchema>;
export const PageSchema = z.object({ id: zPageId, orgId: zOrgId, name: z.string().min(1), slug: z.string().min(1), publicKey: z.string().min(20), status: z.enum(["draft", "published", "archived"]), draftTree: PageTreeSchema, publishedVersionId: zPageVersionId.nullable(), publishedAt: zServerTimestamp.nullable(), createdBy: zUserId, createdAt: zServerTimestamp, updatedAt: zServerTimestamp }); export type Page = z.infer<typeof PageSchema>;
export const PageVersionSchema = z.object({ id: zPageVersionId, orgId: zOrgId, pageId: zPageId, version: z.number().int().positive(), tree: PageTreeSchema, html: z.string(), createdBy: zUserId, createdAt: zServerTimestamp }); export type PageVersion = z.infer<typeof PageVersionSchema>;
export const CreatePageInputSchema = z.object({ id: zPageId, name: z.string().trim().min(1).max(160), slug: z.string().trim().min(1).max(100).regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/) }); export const UpdatePageInputSchema = z.object({ name: z.string().trim().min(1).max(160).optional(), slug: z.string().trim().min(1).max(100).regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/).optional(), draftTree: PageTreeSchema.optional() }); export const PageWriteResponseSchema = z.object({ page: PageSchema, txid: z.number().int() }); export const PublicPageSchema = z.object({ name: z.string(), html: z.string(), publicKey: z.string() });
export type CreatePageInput = z.infer<typeof CreatePageInputSchema>; export type UpdatePageInput = z.infer<typeof UpdatePageInputSchema>;
